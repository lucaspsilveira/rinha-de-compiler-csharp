let fib = fn (n, k1, k2) => {
    let alias = fib;
    let alias2 = alias;
    let alias3 = alias2;
    if (n == 0) {
        k1
    } else {
        if (n == 1) {
            k2
        } else {
            alias3(n - 1, k2, k1 + k2)
        }
    }
};
print(fib(10, 0, 1))